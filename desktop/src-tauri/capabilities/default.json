{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core defaults, plus the OAuth loopback server and permission to hand exactly one URL to the browser: the Supabase authorize endpoint. Not opener:default, which would allow any http(s) URL. Change the scope if the project ever moves to a custom auth domain.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "oauth:allow-start",
    "oauth:allow-cancel",
    {
      "identifier": "opener:allow-open-url",
      "allow": [
        {
          "url": "https://*.supabase.co/auth/v1/authorize*"
        }
      ]
    }
  ]
}
