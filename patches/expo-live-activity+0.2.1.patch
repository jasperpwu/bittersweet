diff --git a/node_modules/expo-live-activity/build/index.d.ts b/node_modules/expo-live-activity/build/index.d.ts
index bdcae0f..f458f21 100644
--- a/node_modules/expo-live-activity/build/index.d.ts
+++ b/node_modules/expo-live-activity/build/index.d.ts
@@ -15,6 +15,14 @@ export type LiveActivityState = {
     imageName?: string;
     dynamicIslandImageName?: string;
     dynamicIslandText?: string;
+    timerStartDateInMilliseconds?: number;
+    isIdle?: boolean;
+    tagId?: string;
+    durationMinutes?: number;
+    startLabel?: string;
+    endLabel?: string;
+    unlockedLabel?: string;
+    unblockExpiredLabel?: string;
 };
 export type NativeLiveActivityState = {
     title: string;
@@ -24,6 +32,14 @@ export type NativeLiveActivityState = {
     imageName?: string;
     dynamicIslandImageName?: string;
     dynamicIslandText?: string;
+    timerStartDateInMilliseconds?: number;
+    isIdle?: boolean;
+    tagId?: string;
+    durationMinutes?: number;
+    startLabel?: string;
+    endLabel?: string;
+    unlockedLabel?: string;
+    unblockExpiredLabel?: string;
 };
 export type LiveActivityConfig = {
     backgroundColor?: string;
@@ -33,6 +49,7 @@ export type LiveActivityConfig = {
     progressViewLabelColor?: string;
     deepLinkUrl?: string;
     timerType?: DynamicIslandTimerType;
+    sessionType?: string;
 };
 export type ActivityTokenReceivedEvent = {
     activityID: string;
@@ -59,11 +76,22 @@ export type LiveActivityModuleEvents = {
  * @returns {string} The identifier of the started activity or undefined if creating live activity failed.
  */
 export declare function startActivity(state: LiveActivityState, config?: LiveActivityConfig): Voidable<string>;
+/**
+ * Atomically reuse-or-create: updates the first still-updatable (non-unlock)
+ * Live Activity with the given state, dismisses every other leftover activity
+ * (including ended-but-still-visible ones), and creates a fresh activity only
+ * if none is reusable. ActivityKit is queried directly, so no activity ID
+ * bookkeeping is needed on the JS side.
+ * @param {LiveActivityState} state The state for the live activity.
+ * @param {LiveActivityConfig} config Live activity config object (used only when a new activity is created).
+ * @returns {string} The identifier of the reused or started activity.
+ */
+export declare function startOrUpdateActivity(state: LiveActivityState, config?: LiveActivityConfig): Voidable<string>;
 /**
  * @param {string} id The identifier of the activity to stop.
  * @param {LiveActivityState} state The updated state for the live activity.
  */
-export declare function stopActivity(id: string, state: LiveActivityState): any;
+export declare function stopActivity(id: string, state: LiveActivityState, dismissImmediately?: boolean): any;
 /**
  * @param {string} id The identifier of the activity to update.
  * @param {LiveActivityState} state The updated state for the live activity.
@@ -72,5 +100,21 @@ export declare function updateActivity(id: string, state: LiveActivityState): an
 export declare function addActivityTokenListener(listener: (event: ActivityTokenReceivedEvent) => void): Voidable<EventSubscription>;
 export declare function addActivityPushToStartTokenListener(listener: (event: ActivityPushToStartTokenReceivedEvent) => void): Voidable<EventSubscription>;
 export declare function addActivityUpdatesListener(listener: (event: ActivityUpdateEvent) => void): Voidable<EventSubscription>;
+/**
+ * Update all active Live Activities with the given state.
+ * Unlike updateActivity, this does not require knowing the activity ID.
+ * @param {LiveActivityState} state The updated state to apply to all activities.
+ */
+export declare function updateAllActivities(state: LiveActivityState): any;
+/**
+ * Ends (dismisses) every focus Live Activity immediately, without needing an
+ * activity ID. Unlock countdowns are left untouched.
+ */
+export declare function endAllFocusActivities(): any;
+/**
+ * Triggers WidgetCenter.shared.reloadAllTimelines() on iOS.
+ * Forces all Home Screen widgets to refresh their timelines immediately.
+ */
+export declare function reloadWidgetTimelines(): any;
 export {};
 //# sourceMappingURL=index.d.ts.map
\ No newline at end of file
diff --git a/node_modules/expo-live-activity/build/index.js b/node_modules/expo-live-activity/build/index.js
index 6b32c30..efcb90c 100644
--- a/node_modules/expo-live-activity/build/index.js
+++ b/node_modules/expo-live-activity/build/index.js
@@ -15,13 +15,27 @@ export function startActivity(state, config) {
     if (assertIOS('startActivity'))
         return ExpoLiveActivityModule.startActivity(state, config);
 }
+/**
+ * Atomically reuse-or-create: updates the first still-updatable (non-unlock)
+ * Live Activity with the given state, dismisses every other leftover activity
+ * (including ended-but-still-visible ones), and creates a fresh activity only
+ * if none is reusable. ActivityKit is queried directly, so no activity ID
+ * bookkeeping is needed on the JS side.
+ * @param {LiveActivityState} state The state for the live activity.
+ * @param {LiveActivityConfig} config Live activity config object (used only when a new activity is created).
+ * @returns {string} The identifier of the reused or started activity.
+ */
+export function startOrUpdateActivity(state, config) {
+    if (assertIOS('startOrUpdateActivity'))
+        return ExpoLiveActivityModule.startOrUpdateActivity(state, config);
+}
 /**
  * @param {string} id The identifier of the activity to stop.
  * @param {LiveActivityState} state The updated state for the live activity.
  */
-export function stopActivity(id, state) {
+export function stopActivity(id, state, dismissImmediately = true) {
     if (assertIOS('stopActivity'))
-        return ExpoLiveActivityModule.stopActivity(id, state);
+        return ExpoLiveActivityModule.stopActivity(id, state, dismissImmediately);
 }
 /**
  * @param {string} id The identifier of the activity to update.
@@ -43,4 +57,21 @@ export function addActivityUpdatesListener(listener) {
     if (assertIOS('addActivityUpdatesListener'))
         return ExpoLiveActivityModule.addListener('onStateChange', listener);
 }
+/**
+ * Update all active Live Activities with the given state.
+ * Unlike updateActivity, this does not require knowing the activity ID.
+ * @param {LiveActivityState} state The updated state to apply to all activities.
+ */
+export function updateAllActivities(state) {
+    if (assertIOS('updateAllActivities'))
+        return ExpoLiveActivityModule.updateAllActivities(state);
+}
+export function endAllFocusActivities() {
+    if (assertIOS('endAllFocusActivities'))
+        return ExpoLiveActivityModule.endAllFocusActivities();
+}
+export function reloadWidgetTimelines() {
+    if (assertIOS('reloadWidgetTimelines'))
+        return ExpoLiveActivityModule.reloadWidgetTimelines();
+}
 //# sourceMappingURL=index.js.map
\ No newline at end of file
diff --git a/node_modules/expo-live-activity/ios-files/LiveActivityView.swift b/node_modules/expo-live-activity/ios-files/LiveActivityView.swift
index e309104..76f3d70 100644
--- a/node_modules/expo-live-activity/ios-files/LiveActivityView.swift
+++ b/node_modules/expo-live-activity/ios-files/LiveActivityView.swift
@@ -54,11 +54,31 @@ import WidgetKit
         }
 
         if let date = contentState.timerEndDateInMilliseconds {
-          Text(timerInterval: Date.toTimerInterval(miliseconds: date))
-            .font(.system(size: 28, weight: .bold, design: .monospaced))
-            .minimumScaleFactor(0.8)
-            .multilineTextAlignment(.leading)
-            .modifier(ConditionalForegroundViewModifier(color: attributes.progressViewLabelColor))
+          let target = Date(timeIntervalSince1970: date / 1000)
+          if target <= Date.now {
+            // Past date: count UP from that time (infinite/elapsed mode)
+            Text(target, style: .timer)
+              .font(.system(size: 28, weight: .bold, design: .monospaced))
+              .minimumScaleFactor(0.8)
+              .multilineTextAlignment(.leading)
+              .modifier(ConditionalForegroundViewModifier(color: attributes.progressViewLabelColor))
+          } else {
+            // Future date: count DOWN to that time (normal mode)
+            // Use timerInterval with the real start date so the displayed second
+            // is computed deterministically from the range, not from render time.
+            // This keeps the Live Activity timer in sync with the home-screen widget.
+            let startDate: Date = {
+              if let startMs = contentState.timerStartDateInMilliseconds {
+                return Date(timeIntervalSince1970: startMs / 1000)
+              }
+              return Date.now
+            }()
+            Text(timerInterval: startDate...target, countsDown: true, showsHours: false)
+              .font(.system(size: 28, weight: .bold, design: .monospaced))
+              .minimumScaleFactor(0.8)
+              .multilineTextAlignment(.leading)
+              .modifier(ConditionalForegroundViewModifier(color: attributes.progressViewLabelColor))
+          }
         } else if let progress = contentState.progress {
           ProgressView(value: progress)
             .tint(progressViewTint)
diff --git a/node_modules/expo-live-activity/ios-files/LiveActivityWidget.swift b/node_modules/expo-live-activity/ios-files/LiveActivityWidget.swift
index fd8eea7..c7f19e9 100644
--- a/node_modules/expo-live-activity/ios-files/LiveActivityWidget.swift
+++ b/node_modules/expo-live-activity/ios-files/LiveActivityWidget.swift
@@ -7,6 +7,7 @@ struct LiveActivityAttributes: ActivityAttributes {
     var title: String
     var subtitle: String?
     var timerEndDateInMilliseconds: Double?
+    var timerStartDateInMilliseconds: Double?
     var progress: Double?
     var imageName: String?
     var dynamicIslandImageName: String?
@@ -21,6 +22,7 @@ struct LiveActivityAttributes: ActivityAttributes {
   var progressViewLabelColor: String?
   var deepLinkUrl: String?
   var timerType: DynamicIslandTimerType?
+  var sessionType: String?
 
   enum DynamicIslandTimerType: String, Codable {
     case circular
@@ -31,16 +33,37 @@ struct LiveActivityAttributes: ActivityAttributes {
 struct LiveActivityWidget: Widget {
   var body: some WidgetConfiguration {
     ActivityConfiguration(for: LiveActivityAttributes.self) { context in
-      if context.isStale {
-        // Timer expired while app was in background — show completion state
+      if context.isStale, context.attributes.sessionType == "unlock" {
+        // Unlock session expired — show "Unblock Expired" (no bonus time)
         VStack(alignment: .leading) {
           HStack(alignment: .center) {
             VStack(alignment: .leading, spacing: 2) {
-              Text("Session Complete")
+              Text("Unblock Expired")
                 .font(.title2)
                 .fontWeight(.semibold)
                 .foregroundStyle(Color(hex: context.attributes.titleColor ?? "#8B4513"))
-              Text("Tap to open app")
+            }
+            Spacer()
+            resizableImage(imageName: context.state.imageName ?? "default-coffee-bean")
+              .frame(maxHeight: 64)
+          }
+        }
+        .padding(24)
+        .activityBackgroundTint(
+          context.attributes.backgroundColor.map { Color(hex: $0) }
+        )
+        .activitySystemActionForegroundColor(Color.black)
+        .applyWidgetURL(from: context.attributes.deepLinkUrl)
+      } else if context.isStale {
+        // Focus session expired — show bonus time count-up
+        VStack(alignment: .leading) {
+          HStack(alignment: .center) {
+            VStack(alignment: .leading, spacing: 2) {
+              Text(context.state.title)
+                .font(.title2)
+                .fontWeight(.semibold)
+                .foregroundStyle(Color(hex: context.attributes.titleColor ?? "#8B4513"))
+              Text("Bonus Time")
                 .font(.title3)
                 .foregroundStyle(Color(hex: context.attributes.subtitleColor ?? "#8B4513").opacity(0.7))
             }
@@ -48,6 +71,18 @@ struct LiveActivityWidget: Widget {
             resizableImage(imageName: context.state.imageName ?? "default-coffee-bean")
               .frame(maxHeight: 64)
           }
+
+          if let endDate = context.state.timerEndDateInMilliseconds {
+            // Date is in the past, so Text(.timer) counts UP automatically
+            HStack(spacing: 4) {
+              Text("+")
+              Text(Date(timeIntervalSince1970: endDate / 1000), style: .timer)
+            }
+            .font(.system(size: 28, weight: .bold, design: .monospaced))
+            .minimumScaleFactor(0.8)
+            .multilineTextAlignment(.leading)
+            .foregroundStyle(Color(hex: "#4CAF7C"))
+          }
         }
         .padding(24)
         .activityBackgroundTint(
@@ -87,7 +122,7 @@ struct LiveActivityWidget: Widget {
         DynamicIslandExpandedRegion(.bottom) {
           if let date = context.state.timerEndDateInMilliseconds {
             dynamicIslandExpandedBottom(
-              endDate: date, progressViewTint: context.attributes.progressViewTint
+              endDate: date, startDateMs: context.state.timerStartDateInMilliseconds, progressViewTint: context.attributes.progressViewTint
             )
             .padding(.horizontal, 5)
             .applyWidgetURL(from: context.attributes.deepLinkUrl)
@@ -116,24 +151,43 @@ struct LiveActivityWidget: Widget {
         }
         .applyWidgetURL(from: context.attributes.deepLinkUrl)
       } compactTrailing: {
-        if context.isStale {
-          Text("Done")
+        if context.isStale, context.attributes.sessionType == "unlock" {
+          // Unlock expired
+          Text("Expired")
             .font(.system(size: 14))
             .fontWeight(.semibold)
+        } else if context.isStale, let date = context.state.timerEndDateInMilliseconds {
+          // Focus session — count up from the expired end date
+          Text(Date(timeIntervalSince1970: date / 1000), style: .timer)
+            .font(.system(size: 15))
+            .minimumScaleFactor(0.8)
+            .fontWeight(.semibold)
+            .foregroundStyle(Color(hex: "#4CAF7C"))
+            .frame(maxWidth: 60)
+            .multilineTextAlignment(.trailing)
         } else if let date = context.state.timerEndDateInMilliseconds {
           compactTimer(
             endDate: date,
+            startDateMs: context.state.timerStartDateInMilliseconds,
             timerType: context.attributes.timerType ?? .circular,
             progressViewTint: context.attributes.progressViewTint
           ).applyWidgetURL(from: context.attributes.deepLinkUrl)
         }
       } minimal: {
-        if context.isStale {
-          Image(systemName: "checkmark.circle.fill")
+        if context.isStale, context.attributes.sessionType == "unlock" {
+          // Unlock expired
+          Image(systemName: "lock.fill")
+            .font(.system(size: 14))
+        } else if context.isStale, let date = context.state.timerEndDateInMilliseconds {
+          // Focus session — count up from the expired end date
+          Text(Date(timeIntervalSince1970: date / 1000), style: .timer)
             .font(.system(size: 14))
+            .fontWeight(.semibold)
+            .foregroundStyle(Color(hex: "#4CAF7C"))
         } else if let date = context.state.timerEndDateInMilliseconds {
           compactTimer(
             endDate: date,
+            startDateMs: context.state.timerStartDateInMilliseconds,
             timerType: context.attributes.timerType ?? .circular,
             progressViewTint: context.attributes.progressViewTint
           ).applyWidgetURL(from: context.attributes.deepLinkUrl)
@@ -145,11 +199,27 @@ struct LiveActivityWidget: Widget {
   @ViewBuilder
   private func compactTimer(
     endDate: Double,
+    startDateMs: Double?,
     timerType: LiveActivityAttributes.DynamicIslandTimerType,
     progressViewTint: String?
   ) -> some View {
-    if timerType == .digital {
-      Text(timerInterval: Date.toTimerInterval(miliseconds: endDate))
+    let target = Date(timeIntervalSince1970: endDate / 1000)
+    if target <= Date.now {
+      // Past date: count UP (infinite/elapsed mode)
+      Text(target, style: .timer)
+        .font(.system(size: 15))
+        .minimumScaleFactor(0.8)
+        .fontWeight(.semibold)
+        .frame(maxWidth: 60)
+        .multilineTextAlignment(.trailing)
+    } else if timerType == .digital {
+      // Use timerInterval with real start date so the countdown is deterministic
+      // and stays in sync with the home-screen widget timer.
+      let startDate: Date = {
+        if let ms = startDateMs { return Date(timeIntervalSince1970: ms / 1000) }
+        return Date.now
+      }()
+      Text(timerInterval: startDate...target, countsDown: true, showsHours: false)
         .font(.system(size: 15))
         .minimumScaleFactor(0.8)
         .fontWeight(.semibold)
@@ -187,11 +257,25 @@ struct LiveActivityWidget: Widget {
     }
   }
 
-  private func dynamicIslandExpandedBottom(endDate: Double, progressViewTint: String?) -> some View {
-    ProgressView(timerInterval: Date.toTimerInterval(miliseconds: endDate))
-      .foregroundStyle(.white)
-      .tint(progressViewTint.map { Color(hex: $0) })
-      .padding(.top, 5)
+  @ViewBuilder
+  private func dynamicIslandExpandedBottom(endDate: Double, startDateMs: Double?, progressViewTint: String?) -> some View {
+    let target = Date(timeIntervalSince1970: endDate / 1000)
+    if target <= Date.now {
+      // Past date: show elapsed time count-up (infinite mode)
+      Text(target, style: .timer)
+        .font(.system(size: 20, weight: .bold, design: .monospaced))
+        .foregroundStyle(.white)
+        .padding(.top, 5)
+    } else {
+      let startDate: Date = {
+        if let ms = startDateMs { return Date(timeIntervalSince1970: ms / 1000) }
+        return Date.now
+      }()
+      ProgressView(timerInterval: startDate...target)
+        .foregroundStyle(.white)
+        .tint(progressViewTint.map { Color(hex: $0) })
+        .padding(.top, 5)
+    }
   }
 
   private func circularTimer(endDate: Double) -> some View {
diff --git a/node_modules/expo-live-activity/ios/ExpoLiveActivityModule.swift b/node_modules/expo-live-activity/ios/ExpoLiveActivityModule.swift
index a222928..61a378d 100644
--- a/node_modules/expo-live-activity/ios/ExpoLiveActivityModule.swift
+++ b/node_modules/expo-live-activity/ios/ExpoLiveActivityModule.swift
@@ -1,5 +1,6 @@
 import ActivityKit
 import ExpoModulesCore
+import WidgetKit
 
 public class ExpoLiveActivityModule: Module {
   struct LiveActivityState: Record {
@@ -28,6 +29,30 @@ public class ExpoLiveActivityModule: Module {
 
     @Field
     var dynamicIslandText: String?
+
+    @Field
+    var timerStartDateInMilliseconds: Double?
+
+    @Field
+    var isIdle: Bool?
+
+    @Field
+    var tagId: String?
+
+    @Field
+    var durationMinutes: Int?
+
+    @Field
+    var startLabel: String?
+
+    @Field
+    var endLabel: String?
+
+    @Field
+    var unlockedLabel: String?
+
+    @Field
+    var unblockExpiredLabel: String?
   }
 
   struct LiveActivityConfig: Record {
@@ -51,6 +76,9 @@ public class ExpoLiveActivityModule: Module {
 
     @Field
     var timerType: DynamicIslandTimerType?
+
+    @Field
+    var sessionType: String?
   }
 
   enum DynamicIslandTimerType: String, Enumerable {
@@ -191,6 +219,18 @@ public class ExpoLiveActivityModule: Module {
       }
 
       do {
+        // End all existing activities before starting a new one to ensure
+        // at most one live activity is shown (e.g. after app kill where JS
+        // couldn't clean up the previous activity).
+        let existingActivities = Activity<LiveActivityAttributes>.activities
+        for existingActivity in existingActivities {
+          let id = existingActivity.id
+          Task {
+            await existingActivity.end(nil, dismissalPolicy: .immediate)
+            print("🧹 Ended existing activity before starting new one: \(id)")
+          }
+        }
+
         let config = maybeConfig ?? LiveActivityConfig()
         let attributes = LiveActivityAttributes(
           name: "ExpoLiveActivity",
@@ -200,22 +240,34 @@ public class ExpoLiveActivityModule: Module {
           progressViewTint: config.progressViewTint,
           progressViewLabelColor: config.progressViewLabelColor,
           deepLinkUrl: config.deepLinkUrl,
-          timerType: config.timerType == .digital ? .digital : .circular
+          timerType: config.timerType == .digital ? .digital : .circular,
+          sessionType: config.sessionType
         )
         let initialState = LiveActivityAttributes.ContentState(
           title: state.title,
           subtitle: state.subtitle,
           timerEndDateInMilliseconds: state.progressBar?.date,
+          timerStartDateInMilliseconds: state.timerStartDateInMilliseconds,
           progress: state.progressBar?.progress,
           imageName: state.imageName,
           dynamicIslandImageName: state.dynamicIslandImageName,
-          dynamicIslandText: state.dynamicIslandText
+          dynamicIslandText: state.dynamicIslandText,
+          isIdle: state.isIdle,
+          tagId: state.tagId,
+          durationMinutes: state.durationMinutes,
+          startLabel: state.startLabel,
+          endLabel: state.endLabel,
+          unlockedLabel: state.unlockedLabel,
+          unblockExpiredLabel: state.unblockExpiredLabel
         )
 
         // Set staleDate to the timer end time so the widget knows when
         // content is outdated (e.g. timer reached 0:00 while app was in background).
-        let timerEndDate: Date? = state.progressBar?.date.map {
-          Date(timeIntervalSince1970: $0 / 1000)
+        // Skip staleDate when the date is in the past (infinite/count-up mode) —
+        // otherwise iOS considers the activity immediately stale and won't show it.
+        let timerEndDate: Date? = state.progressBar?.date.flatMap { ms -> Date? in
+          let d = Date(timeIntervalSince1970: ms / 1000)
+          return d > Date() ? d : nil
         }
 
         let activity = try Activity.request(
@@ -230,32 +282,119 @@ public class ExpoLiveActivityModule: Module {
           await activity.update(ActivityContent(state: newState, staleDate: timerEndDate))
         }
 
-        // Schedule auto-end: sleep until the timer expires, then dismiss.
-        // Task.sleep is suspended when the app is in background, but resumes
-        // when the app returns to foreground — so this acts as a reliable
-        // cleanup that doesn't depend on JS state.
-        if let endDate = timerEndDate {
+
+        return activity.id
+      } catch {
+        throw UnexpectedErrorException(error)
+      }
+    }
+
+    Function("startOrUpdateActivity") {
+      (state: LiveActivityState, maybeConfig: LiveActivityConfig?) -> String in
+      guard #available(iOS 16.2, *) else { throw UnsupportedOSException("16.2") }
+
+      guard ActivityAuthorizationInfo().areActivitiesEnabled else {
+        throw LiveActivitiesNotEnabledException()
+      }
+
+      // Skip staleDate when the date is in the past (infinite/count-up mode) —
+      // otherwise iOS considers the activity immediately stale and won't show it.
+      let timerEndDate: Date? = state.progressBar?.date.flatMap { ms -> Date? in
+        let d = Date(timeIntervalSince1970: ms / 1000)
+        return d > Date() ? d : nil
+      }
+
+      // ActivityKit is the source of truth: reuse the first activity that is
+      // still updatable (the system silently ignores updates to ended
+      // activities) and is not an unlock countdown. Every other leftover —
+      // ended-but-still-visible banners, duplicates, unlock cards — is
+      // dismissed so exactly one activity remains on screen.
+      var reusable: Activity<LiveActivityAttributes>?
+      for activity in Activity<LiveActivityAttributes>.activities {
+        let isUpdatable: Bool
+        switch activity.activityState {
+        case .active, .stale: isUpdatable = true
+        default: isUpdatable = false
+        }
+        if reusable == nil && isUpdatable && activity.attributes.sessionType != "unlock" {
+          reusable = activity
+        } else {
+          let id = activity.id
           Task {
-            let delay = endDate.timeIntervalSinceNow + 1 // 1s buffer
-            if delay > 0 {
-              try? await Task.sleep(nanoseconds: UInt64(delay * 1_000_000_000))
-            }
-            let finalState = LiveActivityAttributes.ContentState(
-              title: "Session Complete",
-              subtitle: nil,
-              timerEndDateInMilliseconds: state.progressBar?.date,
-              progress: nil,
-              imageName: nil,
-              dynamicIslandImageName: nil,
-              dynamicIslandText: nil
-            )
-            await activity.end(
-              ActivityContent(state: finalState, staleDate: nil),
-              dismissalPolicy: .immediate
-            )
-            print("🧹 Auto-ended live activity: \(activity.id)")
+            await activity.end(nil, dismissalPolicy: .immediate)
+            print("🧹 Dismissed leftover activity in startOrUpdateActivity: \(id)")
           }
         }
+      }
+
+      if let activity = reusable {
+        Task {
+          var newState = LiveActivityAttributes.ContentState(
+            title: state.title,
+            subtitle: state.subtitle,
+            timerEndDateInMilliseconds: state.progressBar?.date,
+            timerStartDateInMilliseconds: state.timerStartDateInMilliseconds,
+            progress: state.progressBar?.progress,
+            imageName: nil,
+            dynamicIslandImageName: nil,
+            dynamicIslandText: state.dynamicIslandText,
+            isIdle: state.isIdle,
+            tagId: state.tagId,
+            durationMinutes: state.durationMinutes,
+            startLabel: state.startLabel,
+            endLabel: state.endLabel,
+            unlockedLabel: state.unlockedLabel,
+            unblockExpiredLabel: state.unblockExpiredLabel
+          )
+          try await updateImages(state: state, newState: &newState)
+          await activity.update(ActivityContent(state: newState, staleDate: timerEndDate))
+          print("♻️ Reused activity via startOrUpdateActivity: \(activity.id)")
+        }
+        return activity.id
+      }
+
+      do {
+        let config = maybeConfig ?? LiveActivityConfig()
+        let attributes = LiveActivityAttributes(
+          name: "ExpoLiveActivity",
+          backgroundColor: config.backgroundColor,
+          titleColor: config.titleColor,
+          subtitleColor: config.subtitleColor,
+          progressViewTint: config.progressViewTint,
+          progressViewLabelColor: config.progressViewLabelColor,
+          deepLinkUrl: config.deepLinkUrl,
+          timerType: config.timerType == .digital ? .digital : .circular,
+          sessionType: config.sessionType
+        )
+        let initialState = LiveActivityAttributes.ContentState(
+          title: state.title,
+          subtitle: state.subtitle,
+          timerEndDateInMilliseconds: state.progressBar?.date,
+          timerStartDateInMilliseconds: state.timerStartDateInMilliseconds,
+          progress: state.progressBar?.progress,
+          imageName: state.imageName,
+          dynamicIslandImageName: state.dynamicIslandImageName,
+          dynamicIslandText: state.dynamicIslandText,
+          isIdle: state.isIdle,
+          tagId: state.tagId,
+          durationMinutes: state.durationMinutes,
+          startLabel: state.startLabel,
+          endLabel: state.endLabel,
+          unlockedLabel: state.unlockedLabel,
+          unblockExpiredLabel: state.unblockExpiredLabel
+        )
+
+        let activity = try Activity.request(
+          attributes: attributes,
+          content: .init(state: initialState, staleDate: timerEndDate),
+          pushType: pushNotificationsEnabled ? .token : nil
+        )
+
+        Task {
+          var newState = activity.content.state
+          try await updateImages(state: state, newState: &newState)
+          await activity.update(ActivityContent(state: newState, staleDate: timerEndDate))
+        }
 
         return activity.id
       } catch {
@@ -263,7 +402,7 @@ public class ExpoLiveActivityModule: Module {
       }
     }
 
-    Function("stopActivity") { (activityId: String, state: LiveActivityState) in
+    Function("stopActivity") { (activityId: String, state: LiveActivityState, dismissImmediately: Bool) in
       guard #available(iOS 16.2, *) else { throw UnsupportedOSException("16.2") }
 
       guard
@@ -278,15 +417,23 @@ public class ExpoLiveActivityModule: Module {
           title: state.title,
           subtitle: state.subtitle,
           timerEndDateInMilliseconds: state.progressBar?.date,
+          timerStartDateInMilliseconds: state.timerStartDateInMilliseconds,
           progress: state.progressBar?.progress,
           imageName: nil,
           dynamicIslandImageName: nil,
-          dynamicIslandText: state.dynamicIslandText
+          dynamicIslandText: state.dynamicIslandText,
+          isIdle: state.isIdle,
+          tagId: state.tagId,
+          durationMinutes: state.durationMinutes,
+          startLabel: state.startLabel,
+          endLabel: state.endLabel,
+          unlockedLabel: state.unlockedLabel,
+          unblockExpiredLabel: state.unblockExpiredLabel
         )
         try await updateImages(state: state, newState: &newState)
         await activity.end(
           ActivityContent(state: newState, staleDate: nil),
-          dismissalPolicy: .immediate
+          dismissalPolicy: dismissImmediately ? .immediate : .default
         )
       }
     }
@@ -308,14 +455,82 @@ public class ExpoLiveActivityModule: Module {
           title: state.title,
           subtitle: state.subtitle,
           timerEndDateInMilliseconds: state.progressBar?.date,
+          timerStartDateInMilliseconds: state.timerStartDateInMilliseconds,
           progress: state.progressBar?.progress,
           imageName: nil,
           dynamicIslandImageName: nil,
-          dynamicIslandText: state.dynamicIslandText
+          dynamicIslandText: state.dynamicIslandText,
+          isIdle: state.isIdle,
+          tagId: state.tagId,
+          durationMinutes: state.durationMinutes,
+          startLabel: state.startLabel,
+          endLabel: state.endLabel,
+          unlockedLabel: state.unlockedLabel,
+          unblockExpiredLabel: state.unblockExpiredLabel
         )
         try await updateImages(state: state, newState: &newState)
         await activity.update(ActivityContent(state: newState, staleDate: nil))
       }
     }
+
+    Function("updateAllActivities") { (state: LiveActivityState) in
+      guard #available(iOS 16.2, *) else { throw UnsupportedOSException("16.2") }
+
+      let activities = Activity<LiveActivityAttributes>.activities
+      for activity in activities {
+        // Skip activities that can't be meaningfully updated: the system
+        // silently ignores updates to ended activities, and unlock countdowns
+        // must not be overwritten with focus state.
+        let isUpdatable: Bool
+        switch activity.activityState {
+        case .active, .stale: isUpdatable = true
+        default: isUpdatable = false
+        }
+        guard isUpdatable, activity.attributes.sessionType != "unlock" else { continue }
+        Task {
+          var newState = LiveActivityAttributes.ContentState(
+            title: state.title,
+            subtitle: state.subtitle,
+            timerEndDateInMilliseconds: state.progressBar?.date,
+            timerStartDateInMilliseconds: state.timerStartDateInMilliseconds,
+            progress: state.progressBar?.progress,
+            imageName: nil,
+            dynamicIslandImageName: nil,
+            dynamicIslandText: state.dynamicIslandText,
+            isIdle: state.isIdle,
+            tagId: state.tagId,
+            durationMinutes: state.durationMinutes,
+            startLabel: state.startLabel,
+            endLabel: state.endLabel,
+            unlockedLabel: state.unlockedLabel,
+            unblockExpiredLabel: state.unblockExpiredLabel
+          )
+          try await updateImages(state: state, newState: &newState)
+          await activity.update(ActivityContent(state: newState, staleDate: nil))
+          print("✅ Updated activity \(activity.id) via updateAllActivities")
+        }
+      }
+    }
+
+    // Ends (dismisses) every focus Live Activity immediately, without needing an
+    // activity ID. Unlock countdowns are never touched. Used to dismiss an idle
+    // card that has no resolvable tag instead of rendering a blank "Focus" state.
+    Function("endAllFocusActivities") {
+      guard #available(iOS 16.2, *) else { throw UnsupportedOSException("16.2") }
+
+      for activity in Activity<LiveActivityAttributes>.activities {
+        guard activity.attributes.sessionType != "unlock" else { continue }
+        Task {
+          await activity.end(nil, dismissalPolicy: .immediate)
+          print("🧹 Ended focus activity \(activity.id) via endAllFocusActivities")
+        }
+      }
+    }
+
+    Function("reloadWidgetTimelines") {
+      if #available(iOS 14.0, *) {
+        WidgetCenter.shared.reloadAllTimelines()
+      }
+    }
   }
 }
diff --git a/node_modules/expo-live-activity/ios/LiveActivityAttributes.swift b/node_modules/expo-live-activity/ios/LiveActivityAttributes.swift
index e96360d..f06d359 100644
--- a/node_modules/expo-live-activity/ios/LiveActivityAttributes.swift
+++ b/node_modules/expo-live-activity/ios/LiveActivityAttributes.swift
@@ -6,10 +6,18 @@ struct LiveActivityAttributes: ActivityAttributes {
     var title: String
     var subtitle: String?
     var timerEndDateInMilliseconds: Double?
+    var timerStartDateInMilliseconds: Double?
     var progress: Double?
     var imageName: String?
     var dynamicIslandImageName: String?
     var dynamicIslandText: String?
+    var isIdle: Bool?
+    var tagId: String?
+    var durationMinutes: Int?
+    var startLabel: String?
+    var endLabel: String?
+    var unlockedLabel: String?
+    var unblockExpiredLabel: String?
   }
 
   var name: String
@@ -20,6 +28,7 @@ struct LiveActivityAttributes: ActivityAttributes {
   var progressViewLabelColor: String?
   var deepLinkUrl: String?
   var timerType: DynamicIslandTimerType?
+  var sessionType: String?
 
   enum DynamicIslandTimerType: String, Codable {
     case circular
diff --git a/node_modules/expo-live-activity/src/index.ts b/node_modules/expo-live-activity/src/index.ts
index 2b58e20..59e497d 100644
--- a/node_modules/expo-live-activity/src/index.ts
+++ b/node_modules/expo-live-activity/src/index.ts
@@ -24,6 +24,14 @@ export type LiveActivityState = {
   imageName?: string
   dynamicIslandImageName?: string
   dynamicIslandText?: string
+  timerStartDateInMilliseconds?: number
+  isIdle?: boolean
+  tagId?: string
+  durationMinutes?: number
+  startLabel?: string
+  endLabel?: string
+  unlockedLabel?: string
+  unblockExpiredLabel?: string
 }
 
 export type NativeLiveActivityState = {
@@ -34,6 +42,14 @@ export type NativeLiveActivityState = {
   imageName?: string
   dynamicIslandImageName?: string
   dynamicIslandText?: string
+  timerStartDateInMilliseconds?: number
+  isIdle?: boolean
+  tagId?: string
+  durationMinutes?: number
+  startLabel?: string
+  endLabel?: string
+  unlockedLabel?: string
+  unblockExpiredLabel?: string
 }
 
 export type LiveActivityConfig = {
@@ -44,6 +60,7 @@ export type LiveActivityConfig = {
   progressViewLabelColor?: string
   deepLinkUrl?: string
   timerType?: DynamicIslandTimerType
+  sessionType?: string
 }
 
 export type ActivityTokenReceivedEvent = {
@@ -87,12 +104,26 @@ export function startActivity(state: LiveActivityState, config?: LiveActivityCon
   if (assertIOS('startActivity')) return ExpoLiveActivityModule.startActivity(state, config)
 }
 
+/**
+ * Atomically reuse-or-create: updates the first still-updatable (non-unlock)
+ * Live Activity with the given state, dismisses every other leftover activity
+ * (including ended-but-still-visible ones), and creates a fresh activity only
+ * if none is reusable. ActivityKit is queried directly, so no activity ID
+ * bookkeeping is needed on the JS side.
+ * @param {LiveActivityState} state The state for the live activity.
+ * @param {LiveActivityConfig} config Live activity config object (used only when a new activity is created).
+ * @returns {string} The identifier of the reused or started activity.
+ */
+export function startOrUpdateActivity(state: LiveActivityState, config?: LiveActivityConfig): Voidable<string> {
+  if (assertIOS('startOrUpdateActivity')) return ExpoLiveActivityModule.startOrUpdateActivity(state, config)
+}
+
 /**
  * @param {string} id The identifier of the activity to stop.
  * @param {LiveActivityState} state The updated state for the live activity.
  */
-export function stopActivity(id: string, state: LiveActivityState) {
-  if (assertIOS('stopActivity')) return ExpoLiveActivityModule.stopActivity(id, state)
+export function stopActivity(id: string, state: LiveActivityState, dismissImmediately: boolean = true) {
+  if (assertIOS('stopActivity')) return ExpoLiveActivityModule.stopActivity(id, state, dismissImmediately)
 }
 
 /**
@@ -121,3 +152,29 @@ export function addActivityUpdatesListener(
 ): Voidable<EventSubscription> {
   if (assertIOS('addActivityUpdatesListener')) return ExpoLiveActivityModule.addListener('onStateChange', listener)
 }
+
+/**
+ * Update all active Live Activities with the given state.
+ * Unlike updateActivity, this does not require knowing the activity ID.
+ * @param {LiveActivityState} state The updated state to apply to all activities.
+ */
+export function updateAllActivities(state: LiveActivityState) {
+  if (assertIOS('updateAllActivities')) return ExpoLiveActivityModule.updateAllActivities(state)
+}
+
+/**
+ * Ends (dismisses) every focus Live Activity immediately, without needing an
+ * activity ID. Unlock countdowns are left untouched. Use this to dismiss an
+ * idle card that has no resolvable tag rather than showing a blank "Focus".
+ */
+export function endAllFocusActivities() {
+  if (assertIOS('endAllFocusActivities')) return ExpoLiveActivityModule.endAllFocusActivities()
+}
+
+/**
+ * Triggers WidgetCenter.shared.reloadAllTimelines() on iOS.
+ * Forces all Home Screen widgets to refresh their timelines immediately.
+ */
+export function reloadWidgetTimelines() {
+  if (assertIOS('reloadWidgetTimelines')) return ExpoLiveActivityModule.reloadWidgetTimelines()
+}
